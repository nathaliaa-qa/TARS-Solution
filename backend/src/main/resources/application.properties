spring.application.name=cadastro-usuarios-backend

# ---- Banco de dados PostgreSQL ----
spring.datasource.url=jdbc:postgresql://localhost:5432/db_teste_tecnico
spring.datasource.username=postgres
spring.datasource.password=natheTI95
spring.datasource.driver-class-name=org.postgresql.Driver

spring.jpa.hibernate.ddl-auto=update
spring.jpa.show-sql=true
spring.jpa.properties.hibernate.format_sql=true
spring.jpa.properties.hibernate.dialect=org.hibernate.dialect.PostgreSQLDialect

# ---- JWT ----
# IMPORTANTE: em producao, defina JWT_SECRET via variavel de ambiente (>= 256 bits, base64).
# Nunca commitar um secret real neste arquivo.
jwt.secret=404E635266556A586E3272357538782F413F4428472B4B6250645367566B5970
jwt.expiration-ms=86400000

# ---- ViaCEP ----
viacep.base-url=https://viacep.com.br/ws

# ---- Cache (Caffeine) ----
spring.cache.type=caffeine
spring.cache.cache-names=cepCache
spring.cache.caffeine.spec=maximumSize=500,expireAfterWrite=24h

server.port=${PORT:8080}